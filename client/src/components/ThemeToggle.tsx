import { useTheme } from "@/hooks/use-theme";

// Dark mode is now fixed, so we don't need a theme toggle button
export default function ThemeToggle() {
  // Just return null to hide the toggle since we're using dark mode only
  return null;
}
